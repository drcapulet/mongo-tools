<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "MongoTools" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", "bootstrap-wizard", :media => "all" %>

    <!-- For third-generation iPad with high-resolution Retina display: -->
    <!-- Size should be 144 x 144 pixels -->
    <%= favicon_link_tag 'images/apple-touch-icon-144x144-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '144x144' %>

    <!-- For iPhone with high-resolution Retina display: -->
    <!-- Size should be 114 x 114 pixels -->
    <%= favicon_link_tag 'images/apple-touch-icon-114x114-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '114x114' %>

    <!-- For first- and second-generation iPad: -->
    <!-- Size should be 72 x 72 pixels -->
    <%= favicon_link_tag 'images/apple-touch-icon-72x72-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '72x72' %>

    <!-- For non-Retina iPhone, iPod Touch, and Android 2.1+ devices: -->
    <!-- Size should be 57 x 57 pixels -->
    <%= favicon_link_tag 'images/apple-touch-icon-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png' %>

    <!-- For all other devices -->
    <!-- Size should be 32 x 32 pixels -->
    <%= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %>
  </head>
  <body>
    <div class="wizard" id="setup-wizard">
     
      <h1>MongoTools Setup Wizard</h1>
   
      <div class="wizard-card" data-cardname="welcome">
        <h3>Welcome</h3>
        Let's get started setting up your MongoTools application.
      </div>
   
      <div class="wizard-card" data-cardname="mongo-connection">
        <h3>Mongo Connection</h3>
        
        <div class="wizard-input-section">
          <div class="control-group">
            <select id="mongo-conn-type" tabindex="1" name="mongo[connection][type]">
              <option value="">How is Mongo setup?</option>
              <option value="single">Single</option>
              <option value="multiple">Replica Set and/or Sharding</option>
            </select>

            <span id="mongo-conn-hosts-wrapper" style="display: none;">
              w/
              <input id="mongo-conn-num-hosts" type="number" value="1" class="input-mini" min="1" />
              hosts
            </span>

            <div id="mongo-conn-settings" style="display: none;">
              <div id="mongo-conn-hosts"></div>
              <div id="mongo-conn-opts">
                <!--<label class="checkbox">
                  <input id="mongo-conn-opts-ssl" type="checkbox" name="mongo[connection][opts][ssl]" /> SSL
                </label>-->
                <label class="checkbox">
                  <input id="mongo-conn-opts-auth" type="checkbox" name="mongo[connection][opts][auth]" /> Authentication
                </label>
                <input type="text" class="input-medium" placeholder="Username" id="mongo-conn-auth-user" name="mongo[connection][auth][username]" />
                <input type="text" class="input-medium" placeholder="Password" id="mongo-conn-auth-password" name="mongo[connection][auth][password]" />
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="wizard-card" data-cardname="stats">
        <h3>Statistics</h3>

        MongoTools stores statistics about your database to help display information about past performance and monitor your Mongo setup. It can be configured to use a different configuration or the same as the previous page.<br /><br />

        <div class="wizard-input-section">
          <div class="control-group">
            <select id="stats-conn-type" tabindex="1" name="stats[connection][type]">
              <option value="">How is Mongo setup?</option>
              <option value="same">Same as Previous</option>
              <option value="single">Single</option>
              <option value="multiple">Replica Set and/or Sharding</option>
            </select>

            <span id="stats-conn-hosts-wrapper" style="display: none;">
              w/
              <input id="stats-conn-num-hosts" type="number" value="1" class="input-mini" min="1" />
              hosts
            </span>

            <div id="stats-conn-settings" style="display: none;">
              <div id="stats-conn-hosts"></div>
              <div id="stats-conn-opts">
                <!--<label class="checkbox">
                  <input id="stats-conn-opts-ssl" type="checkbox" name="stats[connection][opts][ssl]" /> SSL
                </label>-->
                <label class="checkbox">
                  <input id="stats-conn-opts-auth" type="checkbox" name="stats[connection][opts][auth]" /> Authentication
                </label>
                <input type="text" class="input-medium" placeholder="Username" id="stats-conn-auth-user" name="stats[connection][auth][username]" />
                <input type="text" class="input-medium" placeholder="Password" id="stats-conn-auth-password" name="stats[connection][auth][password]" />
              </div>
            </div>
          </div>
          <input type="text" class="input-xlarge" placeholder="Database Name" id="stats-conn-database" name="stats[connection][database]" />
        </div>
      </div>

      <div class="wizard-card" data-cardname="finish">
        <h3>Finish</h3>

        Click "Submit" to save the configuration and start using MongoTools!

      </div>

      <div class="wizard-success">
        <h3>Congratulations!</h3>

        Please restart Rails and you'll be all set!
      </div>

      <div class="wizard-error">
        <h3>Error!</h3>

        We're sorry - but some error occured. Please look at the logs and report the bug.
      </div>
     
    </div>

    <%= javascript_include_tag "application", "bootstrap-wizard", "setup" %>
  </body>
</html>
